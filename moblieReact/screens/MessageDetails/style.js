import { StyleSheet } from "react-native"
import {COLORS, PADDING} from '../../tools/constantes'

const detial = StyleSheet.create({
    header: {
        marginTop:30,
    }
})

export default detial
